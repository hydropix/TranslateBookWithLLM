flask
flask-cors
flask-socketio
python-socketio
requests
tqdm
httpx
lxml
python-dotenv
aiofiles

# Audio generation dependencies (optional)
# Uncomment the following lines to enable audiobook generation
# TTS>=0.22.0  # Coqui TTS for text-to-speech
# pydub>=0.25.1  # Audio file manipulation
# torch>=2.0.0  # Required by Coqui TTS (if CUDA available)
# numpy>=1.24.0  # Audio processing